{
  "entities": {
    "User": {
      "title": "User",
      "description": "Admin users with specific rights in the clinic inventory system.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The unique user ID from Firebase Auth."
        },
        "email": {
          "type": "string",
          "description": "User email address."
        },
        "role": {
          "type": "string",
          "enum": ["super_admin", "admin"],
          "description": "The role of the admin: super_admin or standard admin with permissions."
        },
        "permissions": {
          "type": "array",
          "description": "List of categories this admin is authorized to edit. If empty or not set, standard admins cannot edit anything."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Creation timestamp."
        }
      },
      "required": ["uid", "email", "role", "permissions", "createdAt"]
    },
    "Category": {
      "title": "Category",
      "description": "Dynmaic category definitions like Médical, Cuisine, Ménage, Entretien.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Category document ID or name slug."
        },
        "name": {
          "type": "string",
          "description": "Displayed name of the category."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Creation timestamp."
        },
        "staff": {
          "type": "array",
          "description": "Optional list of dynamic personnel members with name, role, and phone for this area."
        }
      },
      "required": ["id", "name", "createdAt"]
    },
    "Product": {
      "title": "Product",
      "description": "A product item stored in the inventory.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Product document ID."
        },
        "name": {
          "type": "string",
          "description": "Name of the product."
        },
        "category": {
          "type": "string",
          "description": "Category name or dynamic Category ID this product belongs to."
        },
        "quantity": {
          "type": "number",
          "description": "Current quantity in stock."
        },
        "minQuantity": {
          "type": "number",
          "description": "Minimum alert limit before stock is flagged as low/rupture."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Creation timestamp."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last update timestamp."
        }
      },
      "required": ["id", "name", "category", "quantity", "minQuantity", "createdAt", "updatedAt"]
    },
    "Supplier": {
      "title": "Supplier",
      "description": "Holds the supplier contact info associated with a product. Access strictly restricted to Super Admins.",
      "type": "object",
      "properties": {
        "productId": {
          "type": "string",
          "description": "Document ID matching the respective product."
        },
        "contacts": {
          "type": "array",
          "description": "Array of contacts, each containing name and phone number."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last updated timestamp."
        }
      },
      "required": ["productId", "contacts", "updatedAt"]
    },
    "StockMovement": {
      "title": "StockMovement",
      "description": "Historical audit log of stock level adjustments, creations, and deletions.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Movement document ID."
        },
        "productId": {
          "type": "string",
          "description": "Product document ID."
        },
        "productName": {
          "type": "string",
          "description": "Name of the product."
        },
        "category": {
          "type": "string",
          "description": "Category of the product."
        },
        "type": {
          "type": "string",
          "enum": ["creation", "adjustment", "deletion"],
          "description": "Operation type."
        },
        "difference": {
          "type": "number",
          "description": "The difference applied (+ or - changes, or positive initial fill)."
        },
        "finalQuantity": {
          "type": "number",
          "description": "Ending stock recorded after adjustment."
        },
        "userEmail": {
          "type": "string",
          "description": "Email of the admin user performing the change."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Time the event was written."
        }
      },
      "required": ["id", "productId", "productName", "category", "type", "difference", "finalQuantity", "userEmail", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Collection mapping authenticated admins to roles and category rights."
    },
    "/categories/{categoryId}": {
      "schema": "Category",
      "description": "Clinic dynamic inventory categories."
    },
    "/products/{productId}": {
      "schema": "Product",
      "description": "Clinic inventory products with name, stock, category."
    },
    "/suppliers/{productId}": {
      "schema": "Supplier",
      "description": "Locked collection containing supplier phone numbers. Visible only to super_admin roles."
    },
    "/stock_movements/{movementId}": {
      "schema": "StockMovement",
      "description": "Historical logs of every product change, read-only restricted to super_admin roles."
    }
  }
}
